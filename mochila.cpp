#include <iostream>
#include <limits>
#include <algorithm>

void mochila(int N, int p[], int b[], int M){
        int** matrix = new int*[N+1];
	for(int i=0; i=N; i++){
		matrix[i] = new int[M+1];
		if(i = 0)
			matrix[i] = 0;
		else
			if()
	}
               

